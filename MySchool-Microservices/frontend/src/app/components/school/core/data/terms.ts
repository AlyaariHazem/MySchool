import { TermEnum } from "../enums/term.enum";
import { ITerm } from "../models/term.model";

export const TERMS: ITerm[] = [
    { id: TermEnum.First, name: 'الأول' },
    { id: TermEnum.Second, name: 'الثاني' }
];